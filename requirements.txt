# DELIBERATELY VULNERABLE — BreachLens SCA test target.
# Every pin below is an OLD version with well-known published CVEs (OSV / Trivy).
Flask==0.12.2
Werkzeug==0.14.1
PyYAML==3.13
requests==2.19.0
Jinja2==2.10
urllib3==1.24.1
Django==1.11.27, 2.2.9, 3.0.1
